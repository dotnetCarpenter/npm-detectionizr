
describe("read path from dos", function() {
	var detect;

	beforeEach(function() {
		detect = require('../detect');
	});

	afterEach(function() {
		detect.destroy();
	});
	
	xit("should resolve in path strings", function() {
		expect(true).toEqual(true);
	});
});